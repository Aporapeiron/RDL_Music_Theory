# 構造抽出：和声機能Module 螺旋型再入循環移植

*対象：229〜238*  
*状態：DRAFT v0.1*

## ■ 1. 接続地図

```text
harmonic function module boundary
× input contract
→ harmonic function input contract candidate

harmonic function input contract candidate
× chord/key payload binding
→ bound harmonic payload candidate

bound harmonic payload candidate
× degree/function validation
→ function processing request candidate

function processing request candidate
× existing 42 function activation
→ function annotation candidate

function annotation candidate
× existing 43 target boundary bridge
→ target candidate boundary observation

candidate observation
× target selection controller
→ selected harmonic target candidate

selected harmonic target candidate
× next context handoff
→ next context interpretation request

next context interpretation request
× contract generalization target
→ harmonic function contract generalization target

harmonic function contract generalization target
× cross-module spiral equivalence check
→ isomorphic spiral shape candidate
```

## ■ 2. 移植で残った形

音程Module 179〜228から和声機能Module 229〜238へ移植して残ったのは、内部語彙ではなく次の境界配置である。

```text
Module-specific input contract
→ payload binding
→ validation
→ processing request
→ existing local activation
→ post-processing boundary
→ handoff / next ξ
→ contract generalization
→ next cycle entry
```

## ■ 3. 確認した非同一性

```text
音程Module spiral
≠ 和声機能Module spiral

interval label / target / voice leading
≠ function annotation / harmonic target / next context

function annotation candidate
≠ target candidate generation
≠ selected target
≠ next context interpretation
≠ terminal closure
```

## ■ 4. 禁止補完

```text
螺旋型再入循環の移植成功
→ 内部語彙の共通化

function annotation candidate
→ target自動生成

selected harmonic target candidate
→ 声部進行実現

contract generalization target
→ Core primitive昇格
```

は行わない。

## ■ 5. 未解決ξ

```text
ξ_cross_module_spiral_equivalence_condition
ξ_harmonic_function_input_contract_surface
ξ_function_processing_request_adoption_controller
ξ_next_context_interpretation_boundary
ξ_harmonic_function_contract_generalization_scope
ξ_third_module_spiral_transfer
```

## ■ 6. 暫定結論

229〜238で、螺旋型再入循環は音程Moduleだけの局所構造ではなく、和声機能Moduleにも移植可能な境界配置候補として観測された。

ただし、移植されたのは処理内容ではなく、契約・束縛・検証・実行・後段境界・handoff・再一般化という配置である。
