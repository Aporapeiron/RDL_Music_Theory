# 20_Music_Structure

Musicへ戻すための構造抽出を置く領域。

責務:

```text
Music実験から音楽固有のB/W/M_Bを抽出する
保存されたものと変化したものを分ける
分析・生成・再聴取からMusic Coreへ返す更新候補を整理する
```

汎用T2機構を抽出する文書は `70_T2_Extraction` 側に置く。

- Melody_Meter/
  旋律×拍節検証からMusic Coreへ返す構造抽出。

- Timbre_Attack/
  音色 / attack検証からMusic Coreへ返す構造抽出。
