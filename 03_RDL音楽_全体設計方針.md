# RDL音楽｜全体設計方針

*RDL音楽理論を構築・検証・拡張する際の設計原則を記録する。*

*状態：DRAFT v0.1*

---

## ■ 0. 目的

RDL音楽理論は、音楽をいきなり文化的な意味や既存の名称から説明しない。

まず、比較的変更されにくく検証しやすい関係から出発し、そこから知覚・学習・文脈・意味へ段階的に上がる。

```text
硬い関係を確認する
        ↓
観測条件を分ける
        ↓
知覚条件を分ける
        ↓
学習された構造を分ける
        ↓
文脈・意味・目的を扱う
```

ここでいう「硬い」とは、絶対に正しいという意味ではない。

```text
硬い
≠
絶対的に正しい
```

現在のBとΓのもとで、関係が比較的安定して検出・再現されるという意味で用いる。

---

## ■ 1. 設計の基本単位

RDL音楽では、対象だけを単独で記述しない。

```text
対象
× B（境界・観測条件）
× Γ（関係抽出・判定規則）
↓
現在扱える関係記述
```

したがって、同じ対象でもBまたはΓが変われば、保存・変化・破断の記述が変わりうる。

### 1.1 B

Bは、その層で何を取り出し、どの範囲を比較するかを定める。

物理側では、少なくとも次を含む。

```text
観測時間窓
時間分解能
サンプリング間隔
観測格子の開始位相・配置
```

知覚側・音楽側では、次のような境界が候補になる。

```text
可聴帯域
時間的統合範囲
音高単位
拍・小節
フレーズ
曲全体
```

Bは固定的な容器ではなく、目的と層に応じて引き直される。

Bは意図的に設定される境界だけでなく、身体・装置・環境によって事実上課される境界も含む。

### 1.2 Γ

Γは、Bの中で何を同じものとして比較し、どの差を破断として扱うかを定める。

例：

```text
Γ_point_ratio       点サンプル上の周波数比を比較する
Γ_periodicity       周期・反復を比較する
Γ_pitch             音高関係を抽出する
Γ_meter             拍・アクセントの関係を抽出する
Γ_context           現在文脈との整合を判定する
```

同じBでもΓが変われば、回収されるM_B候補とξの位置づけが変わる。

Γは、現時点ではRDL音楽_Coreの状態変数ではない。各層で関係を抽出・検証するための明示的な規則として、Adapter・Module・検証系に置く。

### 1.3 M_B

M_Bは、現在のBにおいて同じものとして維持されている関係構造である。対象に永久に埋め込まれた固定属性として最初から確定しない。

一方、検証で扱う「M_B候補」は、M_Bそのものの再定義ではなく、特定のBとΓのもとで関係構造を暫定的に回収した結果である。

現段階では、M_B候補について次の操作的な検証判定を採用する。

```text
M_B候補
=
現在のBとΓのもとで
破断を検出せず、保存として扱える関係記述
```

これは、連続時間や別の観測条件において実際に破断がないことを保証しない。

```text
観測上の保存
≠
対象の絶対的な保存
```

---

## ■ 2. 階層モデル

RDL音楽の全体は、次の四層で設計する。

```text
物理側の観測層
        ↓
基層：身体・知覚条件
        ↓
中核：後天的に学習された音楽構造
        ↓
上層：現在文脈・意味・目的
```

この階層は価値の上下ではなく、主に次の差を分けるためのものである。

```text
何が対象側に近いか
何が形成に時間を要するか
何が現在文脈で変わりやすいか
```

### 2.1 物理側の観測層

波形、周波数、位相、時間変化、成分間の比などを扱う。

この層のFは、対象そのものではなく、検出機器・分析器の \(M_B^device\) を通った作用解釈として扱う。

```text
EFP / physical input
  ↓ M_B^device
F_device
  × B_phys
  × Γ_phys
  ↓
M_B^phys候補
```

したがって、波形検出上のFと、人間の身体・知覚・学習済み音楽構造を通ったFは同一視しない。

```text
F_device ≠ F_human
```

06〜09の波形実験は、主にこの層に属する。

ここで確認するのは音楽的な意味ではなく、次のような関係である。

```text
値が変わっても比が保存される
関係の破断が観測分解能で見落とされる
同じ分解能でも格子配置で検出結果が変わる
```

物理モデルに破断があること、観測時間窓と交差すること、サンプル点で検出されることは別々に記録する。

### 2.2 基層：身体・知覚条件

聴覚器官と知覚系が持つ、比較的深い制約と関係抽出を扱う。
ただし、現段階では基層を直接モデル化しない。ここで扱うFは、少なくとも \(M_B^device\) を通ったFをさらに身体・知覚側の \(M_B^human\) が解釈したものとして読むが、\(M_B^human\) の内部構造を先に確定しない。

基層は、物理側の観測層と中核のlearned音楽構造をそれぞれ詰めた後、その間に残る写像・破断・残差から仮設候補として抽出する。

```text
F_device / physical relation
  ↓
learned music description
  ↓
差分・写像不能・安定写像・破断条件
  ↓
B_base / Γ_base / M_B^base候補
```

したがって、基層を人間知覚の完成モデルとして先取りしない。物理的には違うが同じ音楽カテゴリーへ写るもの、物理的には同じだが綴り・文脈で分岐するもの、検出できるが音楽的には扱われないもの、音楽的には差として扱うが物理記述だけでは回収できないものを、後段の抽出対象として残す。

候補には次がある。

```text
可聴帯域
時間分解能
急激な変化への反応
周期・反復への感度
音のまとまり・分離
```

ただし、これらをすべてM_Bと一括しない。

```text
基層知覚条件
=
B_base
+ Γ_base
+ 基層で比較的安定するM_B候補
```

身体的な入力制約、関係抽出規則、知覚上のまとまりは、必要に応じて分けて記述する。

### 2.3 中核：後天的に学習された音楽構造

経験・反復・教育・参加した音楽環境・文化によって蓄積された関係構造を扱う。

候補には次がある。

```text
音高関係
音程
拍・アクセント
和声
旋律・モチーフ
様式的な予測
```

ここでのM_Bは、誰にとっても普遍的な音楽法則とは限らない。

```text
M_B^learned
=
経験によって学習され、現在の音楽を比較するために再利用される関係構造
```

ここでの`learned`は形成過程を示す名称であり、RDL音楽_Coreの`Core`とは別の概念である。

12平均律、長短調、機能和声、ジャズ理論などは、この層に接続される具体的なM_Bモジュール候補であり、Coreそのものではない。

### 2.4 上層：現在文脈・意味・目的

現在の曲中の文脈、現在の目的、現在の意味解釈、現在の期待を扱う。

```text
M_B^context
=
現在の文脈において一時的に立ち上がっている関係構造
```

文化や経験の蓄積は`M_B^learned`に置き、上層はそれらが現在の曲や状況に応じて立ち上がった状態として扱う。したがって、上層は長期蓄積された構造と同じ固定層ではない。

---

## ■ 3. 層間の接続

基本の流れは下から上である。

```text
波形・物理関係
    ↓
基層の入力・抽出
    ↓
中核の蓄積構造との比較
    ↓
上層の文脈・意味・目的
```

ただし、処理は一方向に固定しない。

```text
上層の期待・目的
        ↓
注意の向け方、B、Γの選択
        ↓
同じ物理入力から異なる関係を回収
```

したがって、上層は物理波形を直接変更しなくても、何を聴き、何を保存し、何を破断とみなすかを変えうる。

この逆方向の作用は、物理側の検証結果と混同せず、知覚・解釈側の仮説として記録する。

---

## ■ 4. 物理側と知覚側の分離

次の三つを同一視しない。

```text
物理的関係が変化した
≠
その変化が現在の観測で検出された
≠
知覚上の破断として感じられた
```

逆方向も同様である。

```text
物理的関係が変化していない
≠
知覚上の保存が成立する
```

知覚上のまとまり、音高、拍、和声、緊張感などを扱う場合は、物理側で使用したB・Γとは別のB・Γを定義する。

06〜09の結論を、そのまま聴覚上の知覚可能性へ拡張しない。

---

## ■ 5. 既知の信号処理とRDL側の成果

実験結果は、次の二層に分けて記録する。

### 5.1 信号処理として既知の部分

```text
周波数比
共通周期
有限観測
短時間現象のサンプル間見落とし
サンプリング格子の位相依存
```

これらをRDL音楽の新定理として主張しない。

### 5.2 RDLによる構造記述

RDL側で得られるのは、既知現象を次の構造へ配置することである。

```text
対象
× B
× Γ
↓
保存／破断として記述された関係
```

この配置から、次の区別が得られる。

```text
値が変わる
≠
関係が変わる

関係が破れる
≠
破断が観測される

保存と判定される
≠
絶対的に保存している
```

この段階での成果は、音楽知覚の完成理論ではなく、音響現象をRDLで記述するための観測・関係抽出層である。

---

## ■ 6. CoreとModuleの境界

Coreには、層をまたいで再利用できる最小文法だけを置く。

```text
B
M_B
W
F
E/H
ξ
Δ
preserve()
change()
stabilize()
destabilize()
shift()
blur()
break()
redistribute()
```

物理側の具体的な検出規則、聴覚モデル、調律、和声、ジャンル、特定の知覚効果は、検証またはModuleとして保持する。

Coreへ昇格する条件は、少なくとも次の通りとする。

```text
複数の層または領域で再利用される
独立した検証条件を持つ
既存Coreの記述だけでは扱えない差を生む
特定文化圏や特定実装に依存しない
```

一つの実験で使われただけの変数や判定規則は、直ちにCoreへ追加しない。

ΓはCoreの状態変数ではなく、各層の関係を抽出・検証するためのAdapter・Module・検証系の規則として管理する。したがって、Coreの状態記述`S_t`にΓを追加する変更ではない。

---

## ■ 7. 検証の進め方

検証は、下位の条件を固定してから上位へ進める。

```text
1. 対象と既知モデルを固定する
2. Bを明示する
3. Γを明示する
4. 保存・変化・破断の判定軸を分離する
5. 結果をM_B候補とξに分けて記録する
6. 別のBまたはΓで結果がどう変わるか確認する
7. その後に知覚・音楽的意味へ接続する
```

一度に複数の条件を変えない。

特に、sample rate、sampling phase、波形モデル、判定規則を同時に変更しない。

実験は、次の順で強度を上げる。

```text
既知関係の再現
→ 関係の時間変化
→ 短時間破断
→ Bの変更
→ Γの変更
→ 未知波形・知覚モデル
```

---

## ■ 8. 破断条件

次の場合は、上位概念を追加する前に設計を引き直す。

- 物理モデルと知覚モデルを同じ結果として扱っている
- Bを固定したまま、観測対象だけが変わったと解釈している
- Γを隠したまま、保存・破断を対象の属性として記述している
- M_Bを絶対属性として扱っている
- M_B候補の操作的な検証判定を、M_Bそのものの定義へすり替えている
- ξを既知の未観測内容そのものとして確定している
- 既知の信号処理をRDL固有の新定理として主張している
- 上層の意味から下位の物理関係を事後的に決めている
- Coreが特定の音楽文化・知覚仮説・実装へ依存している

---

## ■ 9. 現時点の全体像

```text
対象
  ↓
各層のB
（身体・装置・環境による事実上の境界を含む）
  ↓
Γ
（Adapter・Module・検証系の抽出・判定規則）
  ↓
Wを回収・検査
  ↓
M_B候補 / ξ

phys
  ↓
base
  ↓
learned
  ↓
context
```

各層は独立した存在ではなく、下位層から上位層へ関係を渡しながら、上位層からBとΓの選択へ影響を返す。ここでΓは操作・検証側の規則であり、Coreの状態変数へ追加されたことを意味しない。

現在のRDL音楽は、まず物理側の観測・関係抽出層を固め、その後に基層・中核・上層を接続する段階にある。

---

## ■ 10. 最短圧縮

```text
硬い物から始める。
ただし、硬さを絶対真理にしない。

対象 × B × Γ
    ↓
保存／破断として記述された関係

phys
→ base
→ learned
→ context

物理的変化
≠ 観測された変化
≠ 知覚上の破断

Coreは小さく保つ。
Moduleと検証記録で具体化する。
```

---

*v0.1改訂：物理側の波形検証、基層・中核・上層の階層モデル、M_BとM_B候補の分離、Γの配置、CoreとModuleの境界を統合した全体設計方針として改訂。*
