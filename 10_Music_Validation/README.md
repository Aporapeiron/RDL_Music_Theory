# 10_Music_Validation

Music固有の検証を置く領域。

ここでは汎用状態遷移だけを目的にした長大工程列を原則増やさない。実音楽対象に対して、Bを置き、関係を抽出し、保存条件と変化条件を指定し、生成・変形・再聴取まで戻す小さい検証を優先する。

旧 `10_検証` は当面そのまま残し、既存リンクと歴史的検証列を保持する。

- Melody_Meter/
  旋律輪郭保存と拍節・アクセント変位のMusic v0.2検証。
